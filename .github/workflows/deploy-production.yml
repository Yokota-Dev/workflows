name: Deploy Lambda Functions
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref_name }}

      - name: Testing set env
        id: env_vars
        uses: ./.github/actions/get-environment-variables
        with:
          environment: development

      - name: Testing env setted
        run: |
          echo ${{ steps.env_vars.outputs.aws_role_arn }}
          echo ${{ steps.env_vars.outputs.aws_profile }}
          echo ${{ steps.env_vars.outputs.short_env }}
